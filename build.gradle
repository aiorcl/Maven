// Top-level build file where you can add configuration options common to all sub-projects/modules.

buildscript {
    repositories {
        google()
        jcenter()
    }
    dependencies {
        classpath 'com.android.tools.build:gradle:3.5.2'

        // NOTE: Do not place your application dependencies here; they belong
        // in the individual module build.gradle files
    }
}

allprojects {
    repositories {
        google()
        jcenter()

        /*maven {
            url 'file:///D:/Projects/Repository/Star'
        }*/

        /*maven {
            // url 'https://raw.githubusercontent.com/googol113/Stars/master'
            url 'https://github.com/googol113/Stars/raw/master'
        }*/

        maven {
            // url 'https://raw.githubusercontent.com/googol113/Library/master'
            url 'https://github.com/googol113/Library/raw/master'
        }

        maven {
            url 'http://localhost:8081/repository/maven-releases/'
            /*credentials {
                username 'admin'
                password 'googol'
            }*/
        }
    }
}

task clean(type: Delete) {
    delete rootProject.buildDir
}
